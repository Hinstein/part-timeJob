spring:
  datasource:
#    url: jdbc:mysql://120.79.18.242/job?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai
#    username: root
#    password: lilinhai123
    url: jdbc:mysql://localhost/job?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      #更新或者创建数据表结构
      ddl-auto: update
      #控制台显示SQL
    show-sql: true
    open-in-view: false
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  thymeleaf:
    #thymeleaf关闭缓存
    cache: false
  servlet:
    multipart.max-file-size: 10MB
    multipart.max-request-size: 100MB